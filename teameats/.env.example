# Copy this file to .env and fill in your values.
# .env is loaded automatically by Docker Compose.

# SMTP relay (no authentication needed — office network only)
SMTP_HOST=relay.office.lan
SMTP_PORT=25
